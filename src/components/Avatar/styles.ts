import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  avatar: {
    width: 40,
    height: 40,
    borderRadius: 20,
  },
  onlineBubble: {
    position: "absolute",
    width: 10,
    height: 10,
    borderRadius: 5,
  },
});

export default styles;
